/**
 * ⚠️ FILE INI PERLU DILENGKAPI
 *
 * Vitamin Class - Extends Product
 * Implementation Inheritance untuk category-specific products
 */

// TODO: Import Product base class
// const Product = require('./Product');

// TODO: Define Vitamin class extends Product
/*
class Vitamin extends Product {
  constructor(name, price, stock, manufacturer, dosage) {
    super(name, price, stock, 'Vitamin', manufacturer);
    this.dosage = dosage;  // mg
  }
  
  getDosageInfo() {
    return `${this.name} - Dosage: ${this.dosage}mg`;
  }
  
  // Override getInfo untuk include dosage
  getInfo() {
    return `${super.getInfo()} | Dosage: ${this.dosage}mg`;
  }
}
*/

// TODO: Export
// module.exports = Vitamin;
